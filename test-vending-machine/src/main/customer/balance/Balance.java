package customer.balance;

import buyableCondition.*;

public abstract class Balance {
    private String type;
    private Number quantity;
    private BuyableCondition buyableCondition;
    
    private void setBuyableCondition() {
        if(this.toBuy == )
        this.buyableCondition = 
    }

    public Balance(Number quantity) {
        this.quantity = quantity;
    }

    // toBuy가 item의 정보여야 함
    public void payFor(String toBuy) {
        setBuyableCondition()
        if(this.buyableCondition.isNoProblem(this.quantity)) consonson;
    }


}
